name: Smart Port CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  AWS_REGION: us-east-1
  ECR_REGISTRY: ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.us-east-1.amazonaws.com
  TF_VERSION: 1.8.0

jobs:
  # ── 1. Lint & Test ──────────────────────────────────────────────────────────
  lint-and-test:
    name: Lint & Test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service:
          - name: notification-service
            path: smart_port_be/notification_service
            lang: node
          - name: invoice-service
            path: smart_port_be/invoice_service
            lang: node
          - name: logistics-service
            path: smart_port_be/logistics_service
            lang: node
          - name: nest-services
            path: smart_port_be/nest_services
            lang: node
          - name: vessel-tracking-service
            path: smart_port_be/vessel_tracking_service
            lang: go
          - name: berthing-service
            path: smart_port_be/berthing_service
            lang: go
          - name: frontend
            path: smart_port_fe
            lang: node

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        if: matrix.service.lang == 'node'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: ${{ matrix.service.path }}/package-lock.json

      - name: Install Node dependencies
        if: matrix.service.lang == 'node'
        working-directory: ${{ matrix.service.path }}
        run: npm ci

      - name: Build Node service
        if: matrix.service.lang == 'node'
        working-directory: ${{ matrix.service.path }}
        run: npm run build --if-present

      - name: Setup Go
        if: matrix.service.lang == 'go'
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          cache-dependency-path: ${{ matrix.service.path }}/go.sum

      - name: Build Go service
        if: matrix.service.lang == 'go'
        working-directory: ${{ matrix.service.path }}
        run: go build ./...

  # ── 2. Terraform Plan ───────────────────────────────────────────────────────
  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    needs: lint-and-test
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: read
      pull-requests: write

    defaults:
      run:
        working-directory: terraform

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan \
            -var="image_tag=sha-${{ github.sha }}" \
            -var="aws_account_id=${{ secrets.AWS_ACCOUNT_ID }}" \
            -out=tfplan \
            -no-color 2>&1 | tee plan_output.txt
        env:
          TF_VAR_db_password: ${{ secrets.TF_VAR_DB_PASSWORD }}
          TF_VAR_smtp_pass: ${{ secrets.TF_VAR_SMTP_PASS }}

      - name: Upload plan artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-${{ github.sha }}
          path: terraform/tfplan
          retention-days: 7
          if-no-files-found: error

  # ── 3. Terraform Apply ──────────────────────────────────────────────────────
  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: terraform-plan
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: read

    defaults:
      run:
        working-directory: terraform

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init
        run: terraform init

      - name: Download plan artifact
        uses: actions/download-artifact@v4
        with:
          name: tfplan-${{ github.sha }}
          path: terraform/
      
      - name: Verify artifact
        run: |
          if [ ! -f "terraform/tfplan" ]; then
            echo "Error: tfplan artifact not found!"
            ls -la terraform/
            exit 1
          fi

      - name: Terraform Apply
        run: terraform apply -auto-approve tfplan
        env:
          TF_VAR_db_password: ${{ secrets.TF_VAR_DB_PASSWORD }}
          TF_VAR_smtp_pass: ${{ secrets.TF_VAR_SMTP_PASS }}

  # ── 4. Build & Push Docker Images ──────────────────────────────────────────
  build-and-push:
    name: Build & Push Images
    runs-on: ubuntu-latest
    needs: terraform-apply      # ← waits for Terraform to create ECR repos
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
    permissions:
      contents: read

    strategy:
      fail-fast: false
      matrix:
        service:
          - name: notification-service
            path: smart_port_be/notification_service
            ecr_repo: smartport/notification-service
          - name: invoice-service
            path: smart_port_be/invoice_service
            ecr_repo: smartport/invoice-service
          - name: logistics-service
            path: smart_port_be/logistics_service
            ecr_repo: smartport/logistics-service
          - name: nest-services
            path: smart_port_be/nest_services
            ecr_repo: smartport/nest-services
          - name: vessel-tracking-service
            path: smart_port_be/vessel_tracking_service
            ecr_repo: smartport/vessel-tracking-service
          - name: berthing-service
            path: smart_port_be/berthing_service
            ecr_repo: smartport/berthing-service
          - name: frontend
            path: smart_port_fe
            ecr_repo: smartport/frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Generate image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.ECR_REGISTRY }}/${{ matrix.service.ecr_repo }}
          tags: |
            type=sha,prefix=sha-
            type=ref,event=branch
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.service.path }}
          file: ${{ matrix.service.path }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.service.name }}
          cache-to: type=gha,scope=${{ matrix.service.name }},mode=max

      - name: Output image URI
        run: |
          echo "Pushed: ${{ env.ECR_REGISTRY }}/${{ matrix.service.ecr_repo }}:sha-${{ github.sha }}"

  # ── 5. ECS Redeploy ─────────────────────────────────────────────────────────
  ecs-redeploy:
    name: ECS Redeploy
    runs-on: ubuntu-latest
    needs: build-and-push        # ← waits for all images to be pushed
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: read

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Force ECS service redeployment
        run: |
          SERVICES=(
            "notification-service"
            "invoice-service"
            "logistics-service"
            "nest-services"
            "vessel-tracking-service"
            "berthing-service"
            "frontend"
          )
          for svc in "${SERVICES[@]}"; do
            aws ecs update-service \
              --cluster smartport-cluster \
              --service "smartport-${svc}" \
              --force-new-deployment \
              --region ${{ env.AWS_REGION }}
            echo "Triggered redeployment: smartport-${svc}"
          done